---
import { ArrowRight } from "lucide-react";
import { dictionaries } from "~/i18n/dictionaries";
import Layout from "~/layouts/Layout.astro";

const t = dictionaries.fr.notFound;
---

<!--
  The 404 in the same voice as the hero's signature: a large serif
  mark grounded on a hairline rule, a small tracked caption, stated
  plainly, no illustration. "無" (mu, "nothing") stands in for the
  missing page the way "完" closes the footer's colophon.
-->
<Layout lang="fr" title={t.title}>
  <div class="flex min-h-[70vh] flex-col items-center justify-center px-8 text-center sm:px-16">
    <span aria-hidden="true" class="font-serif text-sm text-taupe">無</span>

    <h1 class="mt-2 font-serif text-6xl leading-none text-ink sm:text-8xl">404</h1>

    <div aria-hidden="true" class="mt-6 h-px w-44 bg-stone sm:mt-8 sm:w-56"></div>

    <p class="mt-4 text-xs tracking-[0.3em] text-taupe uppercase sm:text-sm">{t.caption}</p>

    <a
      href="/"
      class="mt-10 inline-flex items-center gap-1 text-xs tracking-[0.3em] text-taupe uppercase transition-colors hover:text-clay"
    >
      {t.back}
      <ArrowRight aria-hidden="true" size={12} strokeWidth={1.5} />
    </a>
  </div>
</Layout>
